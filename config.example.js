const owner = 'your Github organisation/owner'; // e.g. jamessawley
const repo = 'your repo'; // e.g. github-reviewers
const token = 'your personal access token';

module.exports = { owner, repo, token }